# Analysis & Documentation

Incremental, documented data quality checks and analyses for the EEG TikTok study (P4–P31).

## Procedure

Each analysis step is stored in its own timestamped folder:

```
analysis_and_documentation/
├── README.md                          ← this file
├── YYYYMMDD_HHMMSS_<description>/
│   ├── analysis.py                    ← self-contained analysis script
│   ├── description.txt                ← what was done, why, and key findings
│   └── images/                        ← generated figures and tables
├── YYYYMMDD_HHMMSS_<next_step>/
│   ├── ...
```

### Folder naming

`<datetime>_<short_description>` — e.g. `20260309_155600_basic_quality_check`

### Contents of each folder

| File | Purpose |
|------|---------|
| `analysis.py` | Standalone script that reads from `../data/` and writes outputs to `images/` |
| `description.txt` | Plain-text summary: objective, method, findings, any exclusion decisions |
| `images/` | Publication-ready figures and tables generated by the script |

### Running an analysis

```bash
cd analysis_and_documentation/<folder>
python analysis.py
```

All scripts are designed to be run from within their own folder and reference data via relative paths (`../../data/`).

## Analysis Steps

| # | Folder | Description | Status |
|---|--------|-------------|--------|
| 1 | `20260309_155600_basic_quality_check` | Per-participant, per-recording quality metrics: duration, sampling rate, channel coverage, keypress counts | ✅ |
| 2 | `20260309_164100_survey_demographics` | Survey response distributions, age stats, exclusion decisions (neurological disorder, keypress errors) | ✅ |
| 3 | `20260311_145900_exclusion_mask` | A priori exclusion mask: JSON of excluded/included participants with reasons, parametric for future participants | ✅ |
| 4 | `20260311_150000_recording_session_summary` | Per-participant recording metrics with pass/fail: BT loss <20%, duration ≥20min, 100s baseline available | ✅ |
| 5 | `20260311_150100_sample_classification` | Per-participant per-subrecording class block analysis: about_to_skip vs not_about_to_skip sample counts with configurable window/overlap | ✅ |
| 6 | `20260311_153500_per_participant_rf` | **Outdated, No Baseline**: Per-participant RF training (V4 Run-2: 200 trees, depth=7, no notch) on n=25 with train/val box plots and summary table | ✅ |
| 7 | `20260311_194100_per_participant_rf_timeseries` | **Experimental**: Same RF but with full flattened time series (21,504 features) instead of 112 aggregated stats — Option B from Step 6 open decision | ✅ |
| 8 | `20260311_195500_per_participant_transformer_timeseries` | **Outdated, No Baseline & Experimental**: Transformer on full time series (768×28 sequence) — same data pipeline as Step 7, model swap only | ✅ |
| 9 | `20260311_211600_statistical_significance` | **Outdated, No Baseline; Mathematical Proof**: Calculates one-sample t-test (vs 50% baseline) and Cohen's d for the n=25 RF-112 results to justify standardizing the dataset size. | ✅ |
| 10 | `20260311_214000_generalizability_logo_cv_rf` | **Outdated, No Baseline; LOGO-CV**: Evaluates clinical generalizability using Leave-One-Group-Out cross validation across the 25 participants. | ✅ |
| 11 | `20260311_220000_baseline_normalized_rf` | **Baseline Normalization; Current Main Analysis**: Re-runs the Step 6 Intra-Subject RF-112 pipeline, but normalizes all 3-second task features against the single global 100-s resting state mean per participant. | ✅ |
| 12 | `20260311_221000_baseline_normalized_statistical_significance` | **Mathematical Proof; Current Main Analysis**: Calculates one-sample t-test (vs 50% baseline) and Cohen's d for the newly generated baseline normalized RF model (Step 11). | ✅ |
| 13 | `20260311_222000_baseline_normalized_logo_cv_rf` | **Baseline Normalized LOGO-CV; Current Main Analysis**: Re-runs the Step 10 LOGO-CV pipeline using Baseline Normalization to test if removing absolute amplitude bias allows the cross-participant learned state maps to generalize. | ✅ |
| 14 | `20260311_233500_engagement_index_prediction` | **EI Two-Class Prediction; Part of Current Main Analysis**: Trains the identical RF architecture natively on the 5 Standard Engagement Index values (`beta/(alpha+theta)`) to evaluate if traditional metrics can perform short-term micro-decision predictions. Serves as the negative control justifying the 112-feature structural pipeline. | ✅ |
| 15 | `20260311_235800_targeted_explainability` | **Targeted Explainability (Feature Importance); Part of Current Main Analysis**: Extracts Mean Gini feature importances across the 25 Baseline Normalized RF models to identify exactly which neural regions and frequency bands drive predictive performance. | ✅ |
| 16 | `20260312_123320_skip_behavior_bias` | **Skip Class Heterogeneity**: Analyzes the prevalence of rapid consecutive skipping by extracting the lengths of `about_to_skip` blocks across the full included sample (n=25). Finds 85% isolated skips and 15% rapid skip chains. | ✅ |

